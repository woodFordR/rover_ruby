<Hds::PageHeader as |PH|>
  <div class="biggest-h"><PH.Title>adam's collection of things</PH.Title></div>
  <PH.Breadcrumb>
    <Hds::Breadcrumb>
      <Hds::Breadcrumb::Item @text="project" @icon="layers" />
      <Hds::Breadcrumb::Item @text="portfolio" @icon="code" />
    </Hds::Breadcrumb>
  </PH.Breadcrumb>
  <PH.Badges>
    <Hds::Badge @text="working" @icon="rocket" @color="neutral-dark-mode" @iconPosition="trailing" />
  </PH.Badges>
</Hds::PageHeader>
<div class="container">
  <Hds::Separator />
  <Hds::Accordion as |A|>
    {{#each @model as |project|}}
      <A.Item>
        <:toggle>
          <div class="card">
            <Hds::IconTile @logo={{project.logo}} @size="large" />
            <span>{{project.title}}</span>
            <span>{{project.description}}</span>
            <div class="avatar"><img src="{{project.avatar}}" alt="/favicon.png" /></div>
          </div>
        </:toggle>
        <:content>
          <div class="card">
            <div class="code"><img src={{project.codeImage}} alt="/favicon.png" /></div>
              <p>{{project.codeDescription}}</p>
              <p>{{project.owner}}</p>
            </div>
        </:content>
      </A.Item>
      <Hds::Separator />
    {{/each}}
  </Hds::Accordion>
</div>
